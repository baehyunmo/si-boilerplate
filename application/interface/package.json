{
  "name": "@si/interface",
  "version": "0.1.0",
  "description": "DDD Interface Layer — controllers, routes, middleware",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn src/index.ts",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@si/application": "workspace:*"
  },
  "files": [
    "dist"
  ],
  "license": "UNLICENSED",
  "private": true
}
