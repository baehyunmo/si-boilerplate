{
  "name": "@si/domain",
  "version": "0.1.0",
  "description": "DDD Domain Layer — pure business logic, zero external dependencies",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "lint": "eslint src/"
  },
  "files": [
    "dist"
  ],
  "license": "UNLICENSED",
  "private": true
}
